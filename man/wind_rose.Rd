% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_wind_rose.R
\name{wind_rose}
\alias{wind_rose}
\title{Create wind rose diagrams}
\usage{
wind_rose(
  obs,
  data_cols = c(ws = "ws_1hr", wd = "wd_1hr"),
  facet_by = NULL,
  facet_rows = 1,
  wd_nbins = c(16, 8, 4)[1],
  freq_labels_position = 360,
  ws_min = 0,
  ws_step = 2,
  ws_out_units = "m/s",
  fills = "default",
  colour = "black",
  alpha = 0.8,
  bar_width = 1,
  ...
)
}
\arguments{
\item{obs}{A data frame with at least the columns named in `data_cols` and,
when supplied, `facet_by`. Must contain at least one row.}

\item{data_cols}{A named character vector of length 2 mapping the logical
roles `"ws"` (wind speed) and `"wd"` (wind direction) to column names in
`obs`. Defaults to `c(ws = "ws_1hr", wd = "wd_1hr")`.}

\item{facet_by}{A character vector of one or two column names in `obs` used
as faceting variables in [ggplot2::facet_wrap()]. Names, if present, are
used as strip labels. Defaults to `NULL` (no faceting).}

\item{facet_rows}{A single positive integer giving the number of rows in the
facet layout. Ignored when `facet_by` is `NULL`. Defaults to `1`.}

\item{wd_nbins}{Number of directional bins. Must be one of `16`
(N, NNE, NE, ENE, …), `8` (N, NE, E, SE, …), or `4` (N, E, S, W).
Defaults to `16`.}

\item{freq_labels_position}{A single numeric value in \[0, 360\] specifying
the compass bearing (degrees) at which the frequency-axis labels are
placed. Values are snapped to the nearest cardinal direction. Defaults to
`360` (North).}

\item{ws_min}{Minimum wind speed to include. Observations below this
threshold are dropped before plotting. Units are inferred from the data or
from `ws_out_units`. Defaults to `0`.}

\item{ws_step}{Width of each wind speed bin in the units given by
`ws_out_units`. Defaults to `2`.}

\item{ws_out_units}{A single character string specifying the output units for
wind speed (e.g. `"m/s"`, `"km/h"`, `"mph"`). When the wind speed column
does not carry unit metadata it is assumed to be in m/s and converted as
needed. Defaults to `"m/s"`.}

\item{fills}{A character vector of colours for the wind speed fill scale.
Should have at least as many values as there are wind speed bins. Pass
`"default"` to use [ggplot2::scale_fill_viridis_d()]. Defaults to
`"default"`.}

\item{colour}{A single character string giving the colour used for bar
outlines. Defaults to `"black"`.}

\item{alpha}{A single numeric value in \[0, 1\] controlling the opacity of
the bar fills. `0` is fully transparent; `1` is fully opaque. Defaults
to `0.8`.}

\item{bar_width}{A single numeric value in \[0, 1\] controlling the relative
width of each directional bar. A value of `1` makes adjacent bars touch.
Defaults to `1`.}

\item{...}{Additional arguments passed to [ggplot2::geom_col()], such as
`linewidth` or `linetype`.}
}
\value{
A [ggplot2::ggplot()] object.
}
\description{
Generates polar bar charts (wind roses) that summarise the joint
distribution of wind speed and direction. Bars are stacked by wind speed
category and oriented toward the compass direction from which the wind
originates.
}
\examples{
\dontrun{

# Basic usage
wind_rose(example_obs)

# Coarser 8-point rose with output in km/h
wind_rose(example_obs, wd_nbins = 8, ws_out_units = "km/h", ws_step = 10)
}
}
\seealso{
Other Data Visualisation: 
\code{\link{taylor_diagram}()},
\code{\link{tile_plot}()}
}
\concept{Data Visualisation}
